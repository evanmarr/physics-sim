// Placeholder — replaced with the real orbital-mechanics solar system shortly.
export class AstronomyMode {
  constructor(root) {
    this.root = root;
    root.innerHTML = "<div style='padding:20px;color:var(--text-dim)'>Astronomy mode coming online…</div>";
  }
  mount() {}
  unmount() {}
}
